<p id="notice"><%= notice %></p>

<h2><%= @category.name %><h2>
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>

<% @category.resources.each do |resource| %>
  <h4><%= link_to resource.title, resource %><h4>
  <p>Resource rating</p>
<% end %>
